export const OMK_DIR = '.omk';
export const WORKFLOW_MODES = ['deep-interview', 'ralplan', 'ralph'];
export const MODE_TO_AGENT = {
  'deep-interview': 'omk-deep-interview',
  ralplan: 'omk-ralplan',
  ralph: 'omk-ralph',
};
export const DEFAULT_TIMEOUT_MS = 120_000;
export const KIRO_INSTALL_COMMAND = 'curl -fsSL https://cli.kiro.dev/install | bash';
